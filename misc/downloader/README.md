# Downloader

```bash
# setup a venv and install dependencies
python3 -m venv venv && venv/bin/pip install -r downloader-requirements.txt
# test the downloader
venv/bin/python downloader.py -o ~/downloads/downloader-test -w 4 test.url
```
